package com.schoolers.model.whatsapp;

/** Delivery status of a sent WhatsApp message. Includes READ (blue-tick) — no SMS analog. */
public enum WhatsAppLogStatus {
    SENT, DELIVERED, READ, FAILED
}
