package com.schoolers.model.whatsapp;

/** Category of a WhatsApp template — determines which automated trigger (if any) uses it. */
public enum WhatsAppCategory {
    FEE_REMINDER, PAYMENT_CONFIRMATION, RECEIPT_LINK, GENERAL
}
