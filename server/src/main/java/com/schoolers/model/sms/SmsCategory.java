package com.schoolers.model.sms;

/** Category of an SMS template / notification preference — used to group automated triggers (Phase 2). */
public enum SmsCategory {
    FEE_DUE, ABSENCE, EXAM, ADMISSION, HOLIDAY, MEETING, GENERAL
}
