name: Database Backup

on:
  schedule:
    - cron: '0 2 * * *'   # Every day at 2:00 AM UTC
  workflow_dispatch:        # Also allow manual trigger from GitHub UI

jobs:
  backup:
    name: Backup PostgreSQL → Backblaze B2
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ── 1. Install PostgreSQL client tools (pg_dump) ─────────────────────────
      - name: Install pg_dump
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends postgresql-client

      # ── 2. Resolve connection details (auto-fix Supabase IPv6) ─────────────────
      #
      # GitHub Actions runners CANNOT reach IPv6 addresses.
      # Supabase direct DB hosts (db.<ref>.supabase.co) in ap-southeast-1
      # resolve to IPv6 ONLY — there is no A record, only AAAA.
      #
      # This step auto-detects that pattern and transparently switches to the
      # Supabase SESSION pooler (aws-0-<region>.pooler.supabase.com) which:
      #   • is always IPv4
      #   • is pg_dump-compatible (unlike the TRANSACTION pooler on port 6543)
      #   • requires the username to be  postgres.<PROJECT_REF>
      #
      # No secret changes are needed — the project ref is extracted from DB_HOST.
      #
      - name: Resolve connection details (auto-fix Supabase IPv6 hosts)
        run: |
          HOST="${{ secrets.DB_HOST }}"
          PORT="${{ secrets.DB_PORT }}"
          USER="${{ secrets.DB_USERNAME }}"

          # ── Detect Supabase direct host: db.<ref>.supabase.co ────────────────
          if [[ "$HOST" =~ ^db\.([a-zA-Z0-9]+)\.supabase\.co$ ]]; then
            PROJECT_REF="${BASH_REMATCH[1]}"
            echo "⚠️  Detected Supabase direct host — it resolves to IPv6 only."
            echo "   Switching to SESSION pooler (IPv4, pg_dump-compatible)."

            # Session pooler — extract region from host or default to ap-southeast-1
            # Supabase ap-southeast-1 projects all use this pooler endpoint:
            CONNECT_HOST="aws-0-ap-southeast-1.pooler.supabase.com"
            CONNECT_PORT="5432"

            # Pooler requires project ref appended to the username
            if [[ "$USER" != *"."* ]]; then
              CONNECT_USER="${USER}.${PROJECT_REF}"
              echo "   Adjusted username: ${USER} → ${CONNECT_USER}"
            else
              CONNECT_USER="$USER"
            fi

            echo "✅ Using pooler: ${CONNECT_HOST}:${CONNECT_PORT}  user=${CONNECT_USER}"

          else
            # Non-Supabase host, or already a pooler URL — use as-is
            CONNECT_HOST="$HOST"
            CONNECT_PORT="${PORT:-5432}"
            CONNECT_USER="$USER"
            echo "✅ Using configured host: ${CONNECT_HOST}:${CONNECT_PORT}"
          fi

          # Export for subsequent steps
          echo "CONNECT_HOST=${CONNECT_HOST}" >> "$GITHUB_ENV"
          echo "CONNECT_PORT=${CONNECT_PORT}" >> "$GITHUB_ENV"
          echo "CONNECT_USER=${CONNECT_USER}" >> "$GITHUB_ENV"

      # ── 3. Dump the database ─────────────────────────────────────────────────
      - name: Create compressed backup
        env:
          PGPASSWORD: ${{ secrets.DB_PASSWORD }}
        run: |
          TIMESTAMP=$(date +%Y-%m-%d_%H-%M-%S)
          FILENAME="myskoolz_${TIMESTAMP}.dump"

          pg_dump \
            --host="${CONNECT_HOST}" \
            --port="${CONNECT_PORT}" \
            --username="${CONNECT_USER}" \
            --dbname="${{ secrets.DB_NAME }}" \
            --no-password \
            --format=custom \
            --compress=9 \
            --verbose \
            --file="${FILENAME}"

          # Print backup size for the log
          SIZE=$(du -sh "${FILENAME}" | cut -f1)
          echo "BACKUP_FILE=${FILENAME}" >> "$GITHUB_ENV"
          echo "BACKUP_SIZE=${SIZE}"     >> "$GITHUB_ENV"
          echo "✅ Backup created: ${FILENAME} (${SIZE})"

      # ── 4. Upload to Backblaze B2 ─────────────────────────────────────────────
      #
      # Backblaze B2 is S3-compatible, so we reuse the AWS CLI that is
      # pre-installed on every GitHub Actions runner.
      #
      # B2 endpoint format: https://s3.<region>.backblazeb2.com
      # Find your region in the B2 bucket details page (e.g. us-west-004).
      #
      - name: Upload to B2
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.B2_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.B2_APP_KEY }}
        run: |
          aws s3 cp "$BACKUP_FILE" \
            "s3://${{ secrets.B2_BUCKET_NAME }}/backups/${BACKUP_FILE}" \
            --endpoint-url "${{ secrets.B2_ENDPOINT }}" \
            --no-progress

          echo "✅ Uploaded to B2: backups/${BACKUP_FILE} (${BACKUP_SIZE})"

      # ── 5. Delete backups older than 30 days from B2 ─────────────────────────
      #
      # Keeps your B2 bucket clean. Lists all objects, finds ones older than
      # 30 days, and deletes them one by one.
      #
      - name: Prune backups older than 30 days
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.B2_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.B2_APP_KEY }}
        run: |
          CUTOFF=$(date -d '30 days ago' +%s)

          aws s3api list-objects \
            --bucket "${{ secrets.B2_BUCKET_NAME }}" \
            --prefix "backups/" \
            --endpoint-url "${{ secrets.B2_ENDPOINT }}" \
            --query 'Contents[].{Key: Key, LastModified: LastModified}' \
            --output json \
          | jq -r '.[] | "\(.LastModified) \(.Key)"' \
          | while read -r MODIFIED KEY; do
              FILE_TIME=$(date -d "$MODIFIED" +%s)
              if [ "$FILE_TIME" -lt "$CUTOFF" ]; then
                aws s3 rm \
                  "s3://${{ secrets.B2_BUCKET_NAME }}/${KEY}" \
                  --endpoint-url "${{ secrets.B2_ENDPOINT }}"
                echo "🗑️  Deleted old backup: ${KEY}"
              fi
            done

          echo "✅ Pruning complete"

      # ── 6. Summary ───────────────────────────────────────────────────────────
      - name: Print summary
        if: success()
        run: |
          echo "============================================"
          echo "  My-Skoolz Database Backup — SUCCESS"
          echo "============================================"
          echo "  File   : ${BACKUP_FILE}"
          echo "  Size   : ${BACKUP_SIZE}"
          echo "  Bucket : ${{ secrets.B2_BUCKET_NAME }}/backups/"
          echo "  Time   : $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "============================================"

      # ── 7. Notify on failure via email (GitHub built-in) ─────────────────────
      #
      # GitHub automatically emails the repo owner when a scheduled workflow
      # fails — no extra setup needed.  The step below just makes the failure
      # reason visible in the Actions log.
      #
      - name: Log failure reason
        if: failure()
        run: |
          echo "❌ Backup FAILED at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Check the logs above for the exact error."
          echo "GitHub will send a failure email to the repo owner automatically."
